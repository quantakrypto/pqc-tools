# Build output & deps
dist/
**/dist/
node_modules/
coverage/

# Generated type declarations
**/*.d.ts

# Lockfile (npm owns its formatting)
package-lock.json

# Markdown is prose-formatted by hand; don't reflow it.
*.md
**/*.md

# REUSE / licensing data
LICENSES/
REUSE.toml

# Detector benchmark fixtures: intentionally "bad" crypto + exact-byte PEM/SSH
# material; their formatting is part of the test, so don't reflow them.
**/test/benchmark/corpus/

